add_namespace = tdf

# Заглушка: вступительное событие (вызывается фокусом TDF_the_edge_of_the_world)
country_event = {
	id = tdf.1
	title = tdf.1.t
	desc = tdf.1.d
	picture = GFX_report_event_generic_read_write

	is_triggered_only = yes

	option = { # вариант, повышающий обнаружение (заглушка)
		name = tdf.1.a
		set_temp_variable = { TDF_detection_delta = 5 }
		TDF_change_detection = yes
	}

	option = { # вариант, снижающий обнаружение (заглушка)
		name = tdf.1.b
		set_temp_variable = { TDF_detection_delta = -5 }
		TDF_change_detection = yes
	}
}
